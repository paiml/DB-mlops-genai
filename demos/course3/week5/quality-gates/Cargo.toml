[package]
name = "quality-gates"
version = "0.1.0"
edition = "2021"
description = "Production quality gates and orchestration - Course 3 Week 5"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"

[dev-dependencies]
